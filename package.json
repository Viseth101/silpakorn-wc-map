{
  "name": "google-map-api-project",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd Backend && npm install --legacy-peer-deps --omit=dev && npm run start"
  }
}